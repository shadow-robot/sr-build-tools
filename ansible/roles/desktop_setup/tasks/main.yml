---

- name: Install pycurl
  apt: name=python-pycurl state=present

- name: Add PyCharm ppa
  apt_repository: repo='ppa:ubuntu-desktop/ubuntu-make'

- name: Update package lists
  apt: update_cache=yes

- name: Install ubuntu make
  apt: name=ubuntu-make state=present

- name: Install programming tools
  command: sudo umake ide pycharm /root/.local/share/umake/ide/pycharm

- name: Set workspace for pycharm icon
  lineinfile: dest=/root/.local/share/applications/jetbrains-pycharm-ce.desktop regexp=^Exec= line="Exec=bash -i -c \"pycharm\" %F"

- name: Set favorite icons
  shell: dbus-launch --exit-with-session gsettings set com.canonical.Unity.Launcher favorites "['application://ubiquity.desktop', 'application://pycharm.desktop', 'application://terminator.desktop', 'application://nautilus.desktop', 'application://firefox.desktop', 'unity://running-apps', 'unity://expo-icon', 'unity://devices']"
  sudo_user: "{{ros_user}}"